# #5

## 문제 1

### 문제

회사는 Amazon S3에서 정적 웹 사이트를 호스팅하고 DNS에 Amazon Route 53을 사용하고 있습니다.

웹 사이트는 전 세계적으로 수요가 증가하고 있습니다. 회사는 웹사이트에 접속하는 사용자의 대기 시간을 줄여야 합니다.

이러한 요구 사항을 가장 비용 효율적으로 충족하는 솔루션은 무엇입니까?

### 선택지

1. 웹사이트가 포함된 S3 버킷을 모든 AWS 리전에 복제합니다. Route 53 지리적 위치 라우팅 항목을 추가합니다.
2. AWS Global Accelerator에서 액셀러레이터를 프로비저닝합니다. 제공된 IP 주소를 S3 버킷과 연결합니다. 가속기의 IP 주소를 가리키도록 Route 53 항목을 편집합니다.
3. S3 버킷 앞에 Amazon CloudFront 배포를 추가합니다. CloudFront 배포를 가리키도록 Route 53 항목을 편집합니다.
4. 버킷에서 S3 Transfer Acceleration을 활성화합니다. 새 엔드포인트를 가리키도록 Route 53 항목을 편집합니다.

### 정답

3번

### 해설

핵심 요구 사항은 `전 세계 사용자 지연 시간 감소`와 `비용 효율성`입니다.  
`Amazon CloudFront`는 정적 콘텐츠를 전 세계 엣지 로케이션에 캐시해 사용자와 가까운 위치에서 제공하므로, S3 정적 웹사이트의 응답 시간을 줄이는 가장 일반적이고 비용 효율적인 방법입니다.

1번처럼 여러 리전에 버킷을 복제하는 방식은 운영 복잡도와 비용이 커집니다. `AWS Global Accelerator`는 주로 ALB, NLB, EC2 같은 엔드포인트 가속에 적합하고, `S3 Transfer Acceleration`은 주로 업로드 성능 개선용이지 정적 웹사이트 조회 지연 최적화의 대표 해법은 아닙니다.

### 핵심 메모

- `Amazon S3`
  정적 웹사이트 호스팅에 적합한 객체 스토리지다.
- `Amazon Route 53`
  DNS 서비스로 도메인 요청을 적절한 엔드포인트로 라우팅한다.
- `Amazon CloudFront`
  CDN 서비스로 정적 콘텐츠를 엣지 로케이션에 캐시해 전 세계 사용자 지연 시간을 줄인다.

## 문제 2

### 문제

회사는 Amazon API Gateway API에 의해 호출되는 AWS Lambda 함수에서 애플리케이션을 호스팅합니다.

Lambda 함수는 고객 데이터를 Amazon Aurora MySQL 데이터베이스에 저장합니다.

회사에서 데이터베이스를 업그레이드할 때마다 Lambda 함수는 업그레이드가 완료될 때까지 데이터베이스 연결을 설정하지 못합니다.

그 결과 일부 이벤트에 대해 고객 데이터가 기록되지 않습니다.

솔루션 설계자는 데이터베이스 업그레이드 중에 생성된 고객 데이터를 저장하는 솔루션을 설계해야 합니다.

이러한 요구 사항을 충족하는 솔루션은 무엇입니까?

### 선택지

1. Lambda 함수와 데이터베이스 사이에 위치하도록 Amazon RDS 프록시를 프로비저닝합니다. RDS 프록시에 연결하도록 Lambda 함수를 구성합니다.
2. Lambda 함수의 실행 시간을 최대로 늘립니다. 데이터베이스에 고객 데이터를 저장하는 코드에서 재시도 메커니즘을 만듭니다.
3. 고객 데이터를 Lambda 로컬 스토리지에 유지합니다. 고객 데이터를 데이터베이스에 저장하기 위해 로컬 스토리지를 스캔하도록 새로운 Lambda 함수를 구성합니다.
4. 고객 데이터를 Amazon Simple Queue Service(Amazon SQS) FIFO 대기열에 저장합니다. 대기열을 폴링하고 고객 데이터를 데이터베이스에 저장하는 새 Lambda 함수를 생성합니다.

### 정답

4번

### 해설

문제의 핵심은 `데이터베이스 업그레이드 중에도 고객 데이터를 유실하지 않아야 한다`는 점입니다.  
이럴 때는 요청 처리와 데이터베이스 쓰기를 분리하는 비동기 아키텍처가 적합합니다. `Amazon SQS`에 먼저 고객 데이터를 안전하게 적재하고, 별도의 Lambda가 큐를 폴링해 Aurora에 저장하도록 하면 데이터베이스가 일시적으로 사용 불가해도 이벤트를 잃지 않을 수 있습니다.

`RDS Proxy`는 연결 관리와 확장성에는 도움이 되지만, 데이터베이스 자체가 업그레이드로 연결 불가한 동안 데이터를 버퍼링해 주지는 않습니다. 재시도만으로는 장애 시간이 길어질 경우 유실 위험이 남고, Lambda 로컬 스토리지는 영속 저장소가 아니어서 적합하지 않습니다.

### 핵심 메모

- `Amazon API Gateway`
  API 요청을 받아 Lambda로 전달하는 진입점 역할을 한다.
- `AWS Lambda`
  서버리스로 비즈니스 로직을 처리하지만, DB 비가용 시 직접 저장만으로는 유실 위험이 있다.
- `Amazon SQS`
  메시지를 임시 저장해 생산자와 소비자를 분리하고, 장애 상황에서도 데이터 유실을 줄일 수 있다.
- `Amazon Aurora`
  고성능 관계형 데이터베이스지만 업그레이드 중에는 일시적으로 연결이 불가할 수 있다.

## 문제 3

### 문제

회사에 수신 메시지를 수집하는 애플리케이션이 있습니다.

그러면 수십 개의 다른 애플리케이션과 마이크로서비스가 이러한 메시지를 빠르게 사용합니다.

메시지 수는 매우 다양하며 때로는 초당 100,000개로 갑자기 증가하기도 합니다.

회사는 솔루션을 분리하고 확장성을 높이고자 합니다.

어떤 솔루션이 이러한 요구 사항을 충족합니까?

### 선택지

1. Amazon Kinesis Data Analytics에 대한 메시지를 유지합니다. 메시지를 읽고 처리하도록 소비자 애플리케이션을 구성합니다.
2. Auto Scaling 그룹의 Amazon EC2 인스턴스에 수집 애플리케이션을 배포하여 CPU 지표를 기반으로 EC2 인스턴스 수를 확장합니다.
3. 단일 샤드로 Amazon Kinesis Data Streams에 메시지를 씁니다. AWS Lambda 함수를 사용하여 메시지를 사전 처리하고 Amazon DynamoDB에 저장합니다. 메시지를 처리하기 위해 DynamoDB에서 읽을 소비자 애플리케이션을 구성합니다.
4. 여러 Amazon Simple Queue Service(Amazon SQS) 구독이 있는 Amazon Simple Notification Service(Amazon SNS) 주제에 메시지를 게시합니다. 대기열의 메시지를 처리하도록 소비자 애플리케이션을 구성합니다.

### 정답

4번

### 해설

요구 사항은 `수십 개의 소비자`, `급격한 트래픽 증가`, `분리된 아키텍처`, `높은 확장성`입니다.  
`Amazon SNS` 주제에 메시지를 게시하고 각 소비자 또는 마이크로서비스가 자신의 `SQS` 큐를 구독하도록 하면, 퍼블리시/구독 패턴으로 생산자와 소비자를 느슨하게 결합할 수 있습니다.

이 구조는 각 소비자가 독립적으로 확장되고 장애 영향을 격리할 수 있어 마이크로서비스 환경에 잘 맞습니다.  
3번은 단일 샤드 `Kinesis Data Streams`라서 초당 100,000개 급증 트래픽 요구에 맞지 않고, `DynamoDB`를 메시지 배포용 중간 저장소로 쓰는 구조도 비효율적입니다.

### 핵심 메모

- `Amazon SNS`
  대규모 메시지 배포를 위한 퍼블리시/구독 서비스다.
- `Amazon SQS`
  각 소비자가 독립적으로 메시지를 처리할 수 있게 해 주며, 버퍼링과 장애 격리에 유리하다.
- `분리와 확장성`
  `SNS + SQS` 조합은 생산자와 여러 소비자를 느슨하게 결합하는 대표 패턴이다.
- `Kinesis Data Streams`
  대규모 스트리밍 처리에 적합하지만, 보기의 단일 샤드 구성은 요구 처리량에 맞지 않는다.

## 키워드

- S3 Static Website Hosting
- Route 53
- CloudFront
- API Gateway
- Lambda
- SQS
- Aurora MySQL
- SNS
- Pub/Sub
- Decoupling

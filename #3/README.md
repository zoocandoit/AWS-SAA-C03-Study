# #3

## 문제 1

### 문제

한 회사가 AWS에서 여러 Windows 워크로드를 실행합니다.

회사 직원은 두 개의 Amazon EC2 인스턴스에서 호스팅 되는 Windows 파일 공유를 사용합니다.

파일 공유는 서로 간에 데이터를 동기화하고 복제본을 유지합니다.

회사는 사용자가 현재 파일에 액세스 하는 방식을 보존하는 가용성이 높고 내구성이 뛰어난 스토리지 솔루션을 원합니다.

솔루션 설계자는 이러한 요구 사항을 충족하기 위해 무엇을 해야 합니까?

### 선택지

1. 모든 데이터를 Amazon S3로 마이그레이션합니다. 사용자가 파일에 액세스할 수 있도록 IAM 인증을 설정합니다.
2. Amazon S3 파일 게이트웨이를 설정합니다. 기존 EC2 인스턴스에 S3 File Gateway를 탑재합니다.
3. 다중 AZ 구성을 사용하여 파일 공유 환경을 Windows 파일 서버용 Amazon FSx로 확장합니다. 모든 데이터를 FSx for Windows File Server로 마이그레이션합니다.
4. 다중 AZ 구성을 사용하여 파일 공유 환경을 Amazon Elastic File System(Amazon EFS)으로 확장합니다. 모든 데이터를 Amazon EFS로 마이그레이션합니다.

### 정답

3번

### 해설

핵심 요구 사항은 `Windows 파일 공유 방식 유지`, `고가용성`, `내구성`입니다.  
`Amazon FSx for Windows File Server`는 SMB 기반의 완전관리형 Windows 파일 스토리지로, 기존 Windows 파일 공유 방식과 잘 맞고 Multi-AZ 구성을 통해 고가용성도 제공할 수 있습니다.

`Amazon S3`는 객체 스토리지이므로 현재의 파일 공유 접근 방식을 그대로 유지하기 어렵고, `EFS`는 Linux 중심의 NFS 파일 시스템이라 Windows 파일 공유 요구 사항에 맞지 않습니다. 따라서 3번이 가장 적절합니다.

### 핵심 메모

- `Amazon FSx for Windows File Server`
  Windows 네이티브 파일 시스템 기능과 SMB 파일 공유를 제공한다.
- `Multi-AZ`
  고가용성과 내구성을 높이는 데 적합하다.
- `Amazon EFS`
  NFS 기반이라 Linux 워크로드에는 적합하지만 Windows 파일 공유 대체로는 부적절하다.

## 문제 2

### 문제

회사는 최근 청구서에서 Amazon EC2 비용이 증가했음을 확인했습니다.

청구 팀은 몇 가지 EC2 인스턴스에 대한 인스턴스 유형의 원치 않는 수직 확장을 발견했습니다.

솔루션 아키텍트는 최근 2개월간의 EC2 비용을 비교하는 그래프를 생성하고 심층 분석을 수행하여 수직 확장의 근본 원인을 식별해야 합니다.

솔루션 설계자는 최소한의 운영 오버헤드로 정보를 어떻게 생성해야 합니까?

### 선택지

1. AWS 예산을 사용하여 예산 보고서를 생성하고 인스턴스 유형에 따라 EC2 비용을 비교합니다.
2. 비용 탐색기의 세분화된 필터링 기능을 사용하여 인스턴스 유형에 따라 EC2 비용을 심층 분석합니다.
3. AWS Billing and Cost Management 대시보드의 그래프를 사용하여 지난 2개월 동안 인스턴스 유형을 기준으로 EC2 비용을 비교합니다.
4. AWS 비용 및 사용 보고서를 사용하여 보고서를 생성하고 Amazon S3 버킷으로 보냅니다. Amazon S3와 함께 Amazon QuickSight를 소스로 사용하여 인스턴스 유형을 기반으로 대화형 그래프를 생성합니다.

### 정답

2번

### 해설

문제는 `최근 2개월 EC2 비용 비교 그래프`와 `인스턴스 유형별 심층 분석`을 `최소한의 운영 오버헤드`로 수행해야 합니다.  
`AWS Cost Explorer`는 기본적으로 비용 시각화와 필터링, 그룹화 기능을 제공하므로 별도 파이프라인 구축 없이 인스턴스 유형별 비용을 바로 분석할 수 있습니다.

`Cost and Usage Report + QuickSight` 조합은 더 세밀한 맞춤 분석은 가능하지만, 설정과 운영 부담이 더 큽니다. `AWS Budgets`는 예산 추적에 적합하지 근본 원인 분석용 도구는 아닙니다. 따라서 2번이 정답입니다.

### 핵심 메모

- `AWS Cost Explorer`
  비용과 사용량을 시각적으로 분석하고 필터링할 수 있다.
- `AWS Cost and Usage Report`
  세부 데이터 분석에는 강력하지만 운영 오버헤드는 더 크다.
- `최소한의 운영 오버헤드`
  별도 수집·적재·시각화 파이프라인 없이 바로 분석 가능한 서비스를 우선 고려한다.

## 문제 3

### 문제

회사는 1주일 동안 지속되는 예정된 이벤트를 위해 특정 AWS 리전에 있는 3개의 특정 가용 영역에서 보장된 Amazon EC2 용량이 필요합니다.

회사는 EC2 용량을 보장하기 위해 무엇을 해야 합니까?

### 선택지

1. 필요한 리전을 지정하는 예약 인스턴스를 구매합니다.
2. 필요한 리전을 지정하는 온디맨드 용량 예약을 생성합니다.
3. 필요한 리전과 3개의 가용 영역을 지정하는 예약 인스턴스를 구입합니다.
4. 필요한 리전 및 세 개의 가용 영역을 지정하는 온디맨드 용량 예약을 생성합니다.

### 정답

4번

### 해설

요구 사항은 `특정 리전`, `특정 3개 가용 영역`, `1주일 동안 보장된 EC2 용량`입니다.  
`On-Demand Capacity Reservation`은 특정 AZ 단위로 EC2 용량을 예약할 수 있어, 필요한 기간 동안 실제 용량 확보가 가능합니다.

반면 `Reserved Instances`는 주로 비용 할인 혜택을 위한 약정 모델이지, 이 문제처럼 특정 AZ에서 단기 이벤트를 위한 즉각적인 용량 보장을 제공하는 핵심 수단은 아닙니다. 따라서 4번이 맞습니다.

### 핵심 메모

- `On-Demand Capacity Reservation`
  특정 AZ에서 필요한 수량의 EC2 용량을 보장할 수 있다.
- `Reserved Instances`
  비용 최적화 목적이 강하며, 용량 보장 문제의 정답으로 보기 어렵다.
- `특정 AZ 보장`
  시험에서는 `Capacity Reservation`과 연결해서 생각하는 것이 안전하다.

## 키워드

- FSx for Windows File Server
- SMB
- Multi-AZ
- Cost Explorer
- Cost and Usage Report
- On-Demand Capacity Reservation
- Reserved Instances
